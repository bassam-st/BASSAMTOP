# BASSAMTOP 🧠🚀

منصّة مركزية (Dashboard) تجمع كل أنظمة ومشاريع بسّام:
- **Bassam AI Core Engine** (نواة الذكاء الاصطناعي + RAG)
- أدوات الجمارك والمخلص الجمركي
- تطبيقات الفواتير والحساب اليومي
- أدوات أخرى (صوتية، PWA، إلخ)

## 1. مكوّنات المشروع

### 1.1. المجلدات الرئيسية

- `bassam_core/`  
  يحتوي نواة النظام (FastAPI + RAG + المنطق الأساسي).

- `keys/`  
  يحتوي مفاتيح الاتصال (API Keys / Config).  
  **تنبيه مهم:** هذا المجلد يجب أن لا يُرفع إلى GitHub (موجود في `.gitignore`).

### 1.2. ملفات الجذر

- `requirements.txt` – مكتبة بايثون المطلوبة.
- `runtime.txt` – لتحديد إصدار بايثون في بعض المنصّات.
- `render.yaml` – إعداد النشر على Render (انظر أدناه).
- `Dockerfile` – لتشغيل المشروع داخل Docker.
- سكربتات إعداد:
  - `setup-bassam-core-light.sh`
  - `setup-light-with-scheduler.sh`
  - `setup-pwa.sh`
  - `add-chat-ui.sh`
  - `add-memory.sh`

---

## 2. التشغيل المحلي (Local Development)

### 2.1. المتطلبات

- Python 3.10 أو أعلى
- `virtualenv` أو `venv`
- اتصال إنترنت لتحميل الحزم

### 2.2. خطوات التشغيل

```bash
# 1) إنشاء بيئة افتراضية
python -m venv .venv

# 2) تفعيلها
# على لينكس / ماك:
source .venv/bin/activate
# على ويندوز:
# .venv\Scripts\activate

# 3) تثبيت المتطلبات
pip install -r requirements.txt

# 4) إعداد متغيرات البيئة
cp .env.example .env
# عدّل القيم داخل .env (مثل OPENAI_API_KEY, OWNER_PIN, ...)

# 5) تشغيل السيرفر
uvicorn bassam_core.main:app --host 0.0.0.0 --port 8000 --reload
